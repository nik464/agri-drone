# WARNING — DRY-RUN STUBS

**Every file in this folder is a dry-run placeholder.** No training was executed.
All `per_run.jsonl` rows have `status: smoke, metrics: null`.
The `aggregate.json` says `status: dry-run`.

**Do NOT cite any file from this folder as evidence of executed experiments.**

These files document the *planned* experimental design only.
See `configs/matrix/full.yaml` for the intended 2400-cell configuration.
