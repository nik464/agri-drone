# WARNING — DRY-RUN PLACEHOLDER

`baseline_audit.json` in this folder has `status: dry-run` and `metrics: null`.
The shared-recipe EfficientNet-B0 fair audit was **never executed**.

Do NOT cite this file as evidence. Run `evaluate/matrix/audit_baseline.py` on a GPU
host to populate real metrics.
